# package-plus-raw

Use the EHI Atlas workspace package first, then inspect raw/source files only when needed to verify or investigate gaps. Cite structured evidence whenever possible.

Run each prompt in `../../prompts/` and save outputs in this folder.
