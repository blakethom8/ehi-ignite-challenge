# package

Use the EHI Atlas workspace package. Read MANIFEST.json and AGENT-INSTRUCTIONS.md first. Prefer structured evidence, provenance, source contributions, conflicts, and missing-information files.

Run each prompt in `../../prompts/` and save outputs in this folder.
