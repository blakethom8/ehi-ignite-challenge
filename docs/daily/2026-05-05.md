# Daily Log - 2026-05-05

## Implementation Triage

### Downstream chart and assistant reliability pass

**Implemented locally:** Improved the Clinical Insights assistant so the default posture is general chart review rather than pre-op/surgical review, added explicit agent/system context cards, added reset/new-chat controls, moved the first-message composer into the empty state, and made deterministic/context citations follow the question-relevant evidence path. Expanded assistant evidence retrieval to include labs/observations and immunizations so lab questions do not incorrectly report that no chart data is loaded.

**Implemented locally:** Improved the FHIR Charts Clinical Snapshot display for high-signal user feedback: condition and medication dates now show month/year precision, allergy records render as rows instead of hidden chips, and immunizations render visible rows by default.

**Still open:** The larger data-model and workflow items remain: clinical notes from CDR/FHIR exports, provider/site normalization, demographic promotion into published charts, encounter linkage, review queue transparency, dedicated Labs page, richer History filters/details, Care Journey timeline behavior, PDF processor progress fidelity, prepared JSON modal performance, and Publish Chart layout refinements.

## Production QA Notes

### Source Intake prepared JSON modal is laggy and hard to use

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/sources?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`.

**Observed:** Opening the prepared output JSON modal for `cedars-sinai.json` feels laggy. Closing the modal is also slow. The JSON body appears large, and the modal content does not scroll even though the user expects it to be scrollable.

**Likely cause to investigate:** The UI may be fetching and rendering the full prepared JSON into a `<pre>` block even when only a preview is needed. Large JSON rendering inside the modal may block the main thread. The modal also likely needs explicit max-height and internal overflow behavior.

**Todo:** During the next cleanup pass:

- Confirm whether the full prepared JSON is fetched eagerly or only when the modal opens.
- Avoid rendering very large JSON blobs inline by default; consider a truncated preview plus explicit export/download for the full payload.
- Add a scrollable modal body with bounded height.
- Keep export JSON available for users who need the complete structure.
- Test open/close responsiveness on the production Cedars JSON case.

**Status:** Captured for follow-up. No UI changes made yet.

### Clinical Insights needs visible agent profile / system context transparency

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/assistant?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Clinical Insights > Chat, right-side Session Tools.

**Observed:** The assistant response was surprisingly focused on surgeon / pre-procedure review, suggesting that this posture is embedded in the active context or system prompt. The current context panel shows attached library items, settings, and logs, but it does not clearly show the active agent profile/system context that is steering the assistant.

**Product direction:** The chat interface should make the active agent posture transparent. Users should be able to see whether the assistant is operating as a surgical chart reviewer, general chart reviewer, cardiometabolic reviewer, medication safety reviewer, etc. This is similar to the OpenClaw-style idea of having a few explicit agent/system prompt profiles rather than one invisible prompt.

**Todo:** During the next Clinical Insights design pass:

- Add top-of-panel cards for active `Agent Profile` / `System Context` before the Context Library.
- Show the active system posture, scope, and boundaries in readable language.
- Consider 1-2 selectable prompt/profile modes initially, such as `Surgical Review` and `General Chart Review`.
- Make the current prompt or prompt summary inspectable so users understand why the assistant answers the way it does.
- Keep Context Library below the active profile cards; eventually move larger context/profile selection into a modal.
- Ensure response behavior matches the selected profile, especially for non-surgical patient questions.

**Status:** Captured for follow-up. No UI or prompt changes made yet.

### Clinical Insights chat sessions need reset and post-start settings controls

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/assistant?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Clinical Insights > Chat after an initial question has already been asked.

**Observed:** Once a chat session has started, there is no obvious way to restart the conversation, clear the transcript, begin a new session, or adjust the active settings/profile for the next response. This makes it hard to recover from a stale or mis-scoped chat, especially when the assistant appears to be operating under the wrong posture.

**Product direction:** Treat each chat as a session with explicit controls. Users should be able to start a fresh chat, reset attached context, and understand which settings are locked by production defaults versus configurable for the current session.

**Todo:** During the next Clinical Insights cleanup pass:

- Add a visible `New chat` / `Restart session` action near the chat header or Session Tools panel.
- Decide whether reset clears only the transcript, attached context, agent profile, or all session state.
- Let users change session-level profile/settings before the next message, or clearly show when production locks those settings.
- Consider a confirmation if resetting would discard useful session context.
- Add a lightweight session history/log entry so resets are auditable.
- Verify the chat URL, patient workspace, attached context, and model mode remain coherent after reset.

**Status:** Captured for follow-up. No UI or session-state changes made yet.

### Clinical Insights agent says no chart is loaded despite active patient workspace

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/assistant?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Clinical Insights > Chat, after publishing and viewing downstream FHIR Chart data for the same workspace.

**Observed:** The agent response said that there are no charts loaded in the session, even though the selected patient workspace has a published chart and the downstream FHIR Charts pages can render clinical data. This is confusing and undermines trust in the assistant's connection to the patient's record.

**Likely cause to investigate:** The Clinical Insights agent context builder may not be resolving workspace-backed published snapshots the same way FHIR Charts does, or the agent prompt/tooling may be treating attached context packages as the only available chart context. It may also lack clear system instructions that the selected patient workspace and published canonical record are the default data scope.

**Product direction:** The assistant should reliably inherit the selected patient workspace, active published chart snapshot, and approved tool/script surface. It should not require manual "chart attachment" when a patient is already selected. The user-facing model should feel closer to a health-skills workspace: chart data, best-practice scripts, and navigation tools are available by default, with transparent citations and context summaries.

**Todo:** During the next Clinical Insights/backend integration pass:

- Compare the data source used by FHIR Charts with the data source used by the Clinical Insights context builder for workspace patients.
- Ensure the assistant resolves the active published chart snapshot before answering.
- Add a visible "Chart loaded" state in the chat/session panel with patient/workspace name, snapshot/run id, and fact counts.
- Make attached context packages additive, not a replacement for the selected patient's chart.
- Add/verify agent tools or skill-like scripts for querying the canonical record, labs, medications, conditions, encounters, notes, and provenance.
- Update the agent system instructions so it knows the selected patient workspace is its default clinical data scope.
- Add a regression test where a published workspace asks a simple chart-grounded question and the assistant does not claim no chart is loaded.

**Status:** Captured for follow-up. No backend, prompt, or UI changes made yet.

### Clinical Insights chat input is too low, especially before the first message

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/assistant?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Clinical Insights > Chat.

**Observed:** The chat input sits very low at the bottom of the page and is easy to miss. For the first chat / empty conversation state, the input should probably be higher and more central so the user immediately understands where to start.

**Todo:** During the next Clinical Insights UI cleanup pass:

- Shift the chat composer upward or reduce empty vertical dead space.
- For the initial/empty state, consider placing the input around the middle of the chat panel with starter prompts or context chips.
- After messages exist, keep the composer reachable but avoid burying it below a large transcript area.
- Ensure the right session-tools panel does not make the main chat area feel too low or visually secondary.
- Test on common laptop viewports where bottom-fixed inputs are easy to lose.

**Status:** Captured for follow-up. No UI changes made yet.

### Care Journey timeline needs sticky time axis and working encounter expansion

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/care-journey?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Care Journey.

**Observed:** The Encounters group shows a count but does not expand/drop down as expected. When scrolling down the timeline, the x-axis / date context is lost, which makes it hard to understand where each bar sits chronologically.

**Todo:** During the next Care Journey cleanup pass:

- Fix Encounters group expansion so the user can drill into encounter rows like other categories.
- Keep the x-axis timeline/date ruler sticky or duplicated while vertically scrolling.
- Consider a sticky top header for zoom range, current date markers, and key year/month ticks.
- Ensure row expansion and timeline bars stay aligned after scrolling.
- Add a quick smoke check for expanding each major group: medications, conditions, encounters, lab reports, procedures, immunizations.

**Status:** Captured for follow-up. No UI changes made yet.

### Build a dedicated FHIR Charts Labs page

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot > Key Labs. User referenced design inspiration in `/Users/blake/Desktop/Function Screen Shots`.

**Observed:** Lab history currently appears as a section inside Clinical Snapshot. The data is useful, but it is compressed into a general summary page and does not have enough space for longitudinal review, category browsing, detailed trends, or Function Health-style lab interpretation.

**Product direction:** Add a dedicated Labs page/section under FHIR Charts. This should become the primary destination for lab history, replacing the current cramped embedded view with a richer lab-focused experience.

**Todo:** During the next FHIR Charts feature pass:

- Add a Labs navigation item in the FHIR Charts side nav and route structure.
- Design a full-page lab history experience inspired by Function Health-style layouts, while staying consistent with the app's restrained business/clinical design language.
- Include lab category sections such as hematology, metabolic, lipids, thyroid, inflammation, renal, liver, hormones, vitamins/nutrition, and other panels as data supports.
- Show latest value, unit, date, trend/sparkline, reference range, abnormal flags, and source/provenance.
- Support longitudinal lab detail views so users can inspect historical readings for a single marker.
- Handle sparse data cleanly, including "no labs in last 6 months" while still showing older labs.
- Reuse existing Key Labs data where possible, but consider expanding the API shape if needed for richer lab timelines and grouped panels.
- Review screenshots in `/Users/blake/Desktop/Function Screen Shots` before designing/building.

**Status:** Captured for follow-up. No route or UI changes made yet.

### History page filters should be field-based and clinically readable

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/history?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > History table.

**Observed:** The current filter bar filters by `CLASS` and shows opaque class codes such as `13`, `5`, and `DOC`. These are hard to understand. The table also shows `Provider unknown` and source-file identifiers under Provider / Site, so that column is not yet clinically useful. Specialty is not visible.

**Product direction:** The History page should behave like a clean clinical table. Filtering should be based on understandable fields and column-level affordances rather than opaque FHIR class codes. Provider/site/specialty should become useful review dimensions, not raw source leakage.

**Todo:** During the next FHIR Charts History cleanup pass:

- Change the main filter from class-code chips to clinically readable type filters, such as office visit, appointment, lab/diagnostic event, source document event, immunization, procedure, medication, etc.
- Consider column-header filtering as the default interaction: Date, Type, Reason, Provider/Site, Specialty, Resources.
- Avoid a heavy filter modal unless column-header filters become too complex.
- Add provider specialty where available or infer it cautiously from source/encounter metadata.
- Improve Provider / Site display so it does not show `Provider unknown` plus raw source filenames as the main content.
- Keep raw class codes available in a detail/provenance disclosure rather than primary filters.
- Add data-model support if needed so source files, providers, organizations, sites, and encounter types remain separate concepts.

**Status:** Captured for follow-up. No UI or backend changes made yet.

### History encounters need clickable detail/provenance drilldown

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer/history?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > History table.

**Observed:** Encounter rows with `No linked resources` do not provide a useful way to inspect what the encounter is, where it came from, or why it exists in the timeline. The user should be able to click an encounter row/resource cell and see provenance, source data, and helpful encounter metadata even when no labs/conditions/procedures are linked.

**Product direction:** Every timeline row should be inspectable. If the app shows an encounter or source event, it should explain the source, raw identifiers, normalized type/class, provider/site, dates, linked resources, and confidence/provenance.

**Todo:** During the next History page cleanup pass:

- Make encounter rows or the Resources cell clickable.
- Add a detail drawer/modal for encounter/source-event metadata.
- Include source file, source type, raw FHIR id, normalized type, raw class code, provider/site, specialty if available, date/time, extraction/harmonization activity, and linked resources.
- For `No linked resources`, explain whether this is truly an empty encounter, a date-only source event, or a linkage gap.
- Provide links from the detail view to related source contribution/provenance views where available.
- Keep raw ids visible in the detail view, not as primary table labels.

**Status:** Captured for follow-up. No UI changes made yet.

### Allergies panel shows a count but does not render allergy details clearly

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot > Allergies & Sensitivities.

**Observed:** The Allergies & Sensitivities panel header shows `1`, but the body only shows a generic warning and a `No Known Allergies` chip. There is no clear table/list row explaining the allergy record, status, source, onset/date, severity, reaction, or whether this is truly a "no known allergies" assertion.

**Todo:** During the next FHIR Charts cleanup pass:

- Confirm the published workspace API response for allergies and distinguish true allergies from "No Known Allergies" statements.
- Render allergy/sensitivity rows with substance, status, category, reaction, severity/criticality, onset/recorded date, and source/provenance where available.
- If the only record is a "No Known Allergies" assertion, display that as a specific clinical assertion rather than implying a hidden allergy row.
- Make the header count match what is visible in the panel.
- Add an explicit empty state only when there are no allergy records and no no-known-allergies assertion.

**Status:** Captured for follow-up. No UI changes made yet.

### Provider and site-of-service history needs normalization and clinically useful labels

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot > Providers & Sites of Service.

**Observed:** The Provider history section shows `Unknown provider` and raw source filenames such as `8f652cd08a9e-bt_functionhealth_results_11-29-2025.pdf` and `bdcae603b551-cedars-sinai.json`. Sites of service also show source-file labels rather than meaningful organizations, facilities, labs, or collection contexts. This is not clinically usable.

**Product direction:** Provider and site history are key to understanding where care happened and what kind of record each source represents. Lab-only sources, such as commercial lab blood draws or Function Health/Quest-like reports, should be labeled as lab/diagnostic events rather than general providers or sites of service.

**Todo:** During the next data-model / chart cleanup pass:

- Normalize provider, organization, facility, lab, and source names separately instead of collapsing everything into raw filename labels.
- Extract provider/site details from FHIR resources where available: `Practitioner`, `Organization`, `Encounter.serviceProvider`, `DiagnosticReport.performer`, `Observation.performer`, `DocumentReference.author`, and source metadata.
- Add source-type semantics: lab blood draw, health system portal export, PDF lab report, office visit, procedure report, imaging report, pharmacy/payer record, etc.
- Avoid displaying raw storage filenames as primary clinical labels. Keep them in provenance/details only.
- Improve Provider history and Sites of service cards so they show useful labels, counts, last seen date, and source categories.
- Add tests for lab-only sources so they appear as lab/diagnostic service events rather than unknown providers.

**Status:** Captured for follow-up. No backend or UI changes made yet.

### Immunizations panel shows counts but no visible immunization rows

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot > Immunizations.

**Observed:** The Immunizations panel header shows `10 total · 8 unique`, but the panel body does not show the immunization list on screen. This makes it look like the data exists in the summary but is not rendered or is hidden/collapsed incorrectly.

**Todo:** During the next FHIR Charts cleanup pass:

- Confirm whether immunization data is present in the published workspace API response.
- Fix the Clinical Snapshot immunizations panel so it renders the vaccine names, dates, status, and CVX/source details.
- Ensure collapsed/expanded state is obvious and does not hide content without a clear affordance.
- Add an empty state only when the true count is zero.
- Add a regression test or smoke check for a workspace with immunizations.

**Status:** Captured for follow-up. No UI changes made yet.

### Published workspace demographic fields are not flowing into FHIR Charts

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot after publishing the workspace.

**Observed:** The published workspace loads downstream chart content, encounter dates, conditions, medications, and data span, but the demographic summary is mostly blank or generic: `Workspace · 0 years old · Location unknown`, with Date of Birth, Race, Ethnicity, Language, and Marital Status empty.

**Likely cause to investigate:** The published workspace reader / canonical facade is probably deriving the patient summary from the workspace profile name instead of promoting demographic fields from the source FHIR/JSON Patient resource. The source export may contain demographics, but the harmonized/published layer is not carrying them through to the frontend patient summary model.

**Todo:** During the next data connection pass:

- Inspect the uploaded Cedars JSON / FHIR source for `Patient` resource demographics, including birth date, gender, address/location, race, ethnicity, language, marital status, and contact fields.
- Confirm whether the harmonization run artifact includes Patient resources or whether patient demographics are dropped before publish.
- Extend the published workspace facade so FHIR Charts reads demographics from the active canonical snapshot when available.
- Preserve workspace display name separately from patient demographics; do not let a workspace name overwrite clinical identity fields.
- Add tests for a published workspace with a Patient resource so DOB/age/location and other demographic fields populate downstream.
- Decide which demographic fields are safe and useful in the demo UI versus private/sensitive fields that should remain hidden or masked.

**Status:** Captured for follow-up. No backend or UI changes made yet.

### Conditions and medications tables need month/year date precision

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot.

**Observed:** The Conditions table `Onset` column and Medications table `Ordered` column currently show only the year, such as `2026` or `2025`. That is too coarse for clinical review and makes same-year ordering hard to interpret.

**Todo:** During the next FHIR Charts cleanup pass:

- Show at least month + year in the Conditions `Onset` column and Medications `Ordered` column when month precision is available.
- Use a consistent compact format such as `Apr 2026` or `2026-04`.
- Preserve day precision where useful if the source has it, but avoid over-claiming precision if the source only provides year.
- Add a fallback display for unknown or partial dates.

**Status:** Captured for follow-up. No UI changes made yet.

### Encounter classification and fact linkage look inconsistent in FHIR Charts

**Context:** Production app at `https://ehi.healthcaredataai.com/explorer?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, FHIR Charts > Clinical Snapshot / Care activity.

**Observed:** The care activity view separates items such as `Office Visit` and `Appointment`, but it is not clear why these are categorized differently. The cards also show `0 labs/vitals`, `0 conditions`, and `0 procedures` even though the same published workspace clearly has conditions, medications, labs, and source facts elsewhere. This makes the encounter timeline feel disconnected from the actual clinical record.

**Likely cause to investigate:** Encounter class/type normalization and resource-to-encounter linkage may not be strong enough for published workspace data. Some facts may have dates but no encounter references, or the published canonical facade may synthesize encounters without linking observations/conditions/procedures back to them. The `DOC` / harmonized source fact encounter also suggests document-derived facts may be represented as pseudo-encounters without clear semantics.

**Todo:** During the next data-model / chart cleanup pass:

- Review how encounter class, encounter type, and display label are derived for uploaded/published workspaces.
- Decide whether `Office Visit` and `Appointment` should be merged, renamed, or kept separate with clearer definitions.
- Audit how observations, conditions, procedures, medications, notes, and document artifacts link to encounters in the published workspace facade.
- If no explicit encounter reference exists, consider date-based grouping into source/document events rather than showing empty encounter cards.
- Improve the Care activity cards so they show clinically meaningful linked facts or clearly state that a date/source event has no linked facts.
- Add tests for published workspaces with encounter-linked and date-only facts so downstream FHIR Charts do not silently show zero linked content.

**Status:** Captured for follow-up. No backend or UI changes made yet.

### Data model coverage gap: clinical notes from JSON/FHIR exports are not visible

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/harmonize?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Harmonized Record > Source Contributions for `cedars-sinai.json`.

**Observed:** The Cedars JSON source contributes labs, conditions, medications, allergies, and immunizations, but clinical notes / narrative document content are not visible in the contribution breakdown or review surfaces. This suggests the harmonized data model is still missing important parts of the structured export, especially notes and document narrative content.

**Product direction:** Continue expanding the canonical data model beyond the initial USCDI-style core facts. Clinical notes are high-value because they often explain why a condition, medication, procedure, or clinical decision matters. They should be preserved with provenance, dates, author/source metadata, encounter links, and document type.

**Todo:** During the next data-model pass:

- Inspect the Cedars JSON/FHIR export for note-bearing resources and fields, including `DocumentReference`, `Composition`, `DiagnosticReport.presentedForm`, `Encounter.reasonCode`, narrative `text.div`, attachments, and any provider note payloads.
- Add first-class clinical note / document artifact handling in the harmonized candidate model.
- Preserve note metadata: date, document type, author/organization, encounter reference, source file, extraction method, and confidence.
- Decide what belongs in the canonical record versus source-attached context. Notes should probably remain provenance-backed source artifacts with structured links to facts and encounters rather than being flattened into diagnoses.
- Add UI coverage so Source Contributions and Review Queue can show clinical notes/documents alongside labs, conditions, medications, allergies, immunizations, procedures, and reports.
- Add tests with Cedars-like JSON and synthetic note-bearing FHIR bundles so the parser does not silently drop notes.

**Status:** Captured for follow-up. No UI or parser changes made yet.

### Publish Chart page should prioritize the activation action and rebalance layout

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/publish?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Publish Chart page.

**Observed:** The "Publish canonical record" action is the key final step, but it feels visually buried inside the right-hand card and is not noticeable enough. The page also feels slightly inverted: snapshot history takes the larger left column even when there are no snapshots, while the publish latest run workflow sits in the narrower right column.

**Product direction:** The publish page should make the active decision obvious: "This latest reviewed run is ready to become the chart used by FHIR Charts and Clinical Insights." Snapshot history is important, but it is secondary unless the user is rolling back or auditing prior publications.

**Todo:** During the next UI cleanup pass:

- Move "Publish latest run" into the primary left column or otherwise give it first visual priority.
- Move "Snapshot history" to the right rail / secondary column.
- Make the primary publish button more prominent and closer to the run summary.
- Consider a top-level sticky or callout action when the latest run is publishable and no active snapshot exists.
- Keep rollback/history visible but visually secondary.
- Clarify button copy and surrounding text so users understand this activates downstream app surfaces.

**Status:** Captured for follow-up. No UI changes made yet.

### Data review needs a timeline or encounter-centered lens

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/harmonize?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Harmonized Record > Source Contributions / reverse provenance view for `cedars-sinai.json`.

**Observed:** The source contribution view is useful for seeing what a source contributed by resource type, but it is mostly a list of conditions, medications, allergies, immunizations, and labs. For clinical review, dates and encounter sequence matter. It is hard to understand the patient story or validate the source in context without seeing when each record happened and how records cluster around encounters or clinical periods.

**Product direction:** Add a compact timeline or encounter-oriented review surface inside the Data Aggregator workflow. This should not replace FHIR Charts, but should give reviewers a quick temporal stamp of what was captured from the source and what will feed the canonical record.

**Todo:** During the next cleanup/design pass:

- Explore a "Source Timeline" or "Encounter Review" section in Harmonized Record, especially under Source Contributions or Review Queue.
- Show key dated records from the selected source: encounters, conditions, medication starts, immunizations, labs, procedures, and clinical notes.
- Group by encounter/date where possible, with source and confidence badges.
- Highlight extracted PDF facts separately from structured FHIR facts while preserving provenance.
- Make it easy to answer: "What happened when?", "Which source said it?", and "Is this clinically plausible?"
- Reuse patterns from FHIR Charts timeline/care journey where they already work, but keep this view focused on data validation and source review.

**Status:** Captured for follow-up. No UI changes made yet.

### Review queue conflicts need human-readable clinical context and clearer actions

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/harmonize?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Harmonized Record > Review Queue after running harmonization.

**Observed:** The review queue shows a same-day lab value conflict for Pulse, but the values under review are mostly presented as source filenames plus opaque FHIR Observation ids. It is not clear what observation/fact/encounter is being discussed, why the values are conflicting, what the recommended action is, or what "Accept candidate" will actually do.

**Likely cause to investigate:** The review item model appears to carry enough source references to list conflicting facts, but the UI is not enriching those references with the clinical display name, encounter context, source type, extraction confidence, source timestamp, candidate winner, rejected alternatives, or recommendation rationale.

**Todo:** During the next cleanup pass:

- Replace opaque Observation ids as the primary label with human-readable fact labels such as `Pulse`, `Heart rate`, source, value, unit, date, and encounter/source context.
- Show the candidate decision explicitly: which value would become canonical, which values would be ignored/retained as alternates, and why.
- Add a recommendation/rationale area such as "Prefer the most recent structured source" or "Needs manual review because same timestamp has conflicting values."
- Rename or clarify `Accept candidate` so the user understands the effect.
- Add actions for common outcomes: accept candidate, choose another value, keep all as separate observations, mark source issue, or defer.
- Preserve provenance by keeping the raw resource ids available in a secondary details disclosure, not as the main content.

**Status:** Captured for follow-up. No UI changes made yet.

### PDF processor progress UI jumps instead of showing meaningful page progress

**Context:** Production app at `https://ehi.healthcaredataai.com/aggregate/sources?patient=workspace-b2627001-0f7d-4204-bc81-4cd779b717f8`, Source Intake PDF processor panel.

**Observed:** While running the PDF processor, the progress display quickly jumps from a low percentage such as 11-15% to complete. The page processing map shows detected pages, but the "pages complete" count does not visibly cycle through each page before completion.

**Likely cause to investigate:** The backend extraction job probably exposes only coarse job states rather than per-page events, so the frontend is estimating progress from status and file/page metadata. If extraction completes between polling intervals, the UI jumps straight from early synthetic progress to done.

**Todo:** During the next cleanup pass:

- Inspect the extract job response fields and confirm whether actual per-page progress is available.
- If backend only exposes coarse states, rename the UI from exact page progress to an estimated processing state.
- If feasible, emit page-level progress from the PDF processor job so the frontend can show `1/5`, `2/5`, etc.
- Keep the page map, but make it honest: pending, processing, parsed, or completed based on real events.
- Validate behavior on a small 5-page PDF and a larger PDF in production.

**Status:** Captured for follow-up. No UI changes made yet.
