FROM python:3.13-slim

WORKDIR /app

# Install uv for fast dependency resolution
RUN pip install --no-cache-dir uv

# Copy dependency files first for layer caching
COPY pyproject.toml uv.lock ./

# Install dependencies
RUN uv sync --frozen --no-dev

# Copy application code
COPY lib/ ./lib/
COPY api/ ./api/
COPY scripts/ ./scripts/
COPY ehi-atlas/ehi_atlas/ ./ehi-atlas/ehi_atlas/
# Reference vocabulary slices (LOINC, RxNorm, CVX) — read by the PDF lab
# pipeline's loinc_matcher and the bundle packager's terminology builder.
COPY ehi-atlas/corpus/reference/ ./ehi-atlas/corpus/reference/

EXPOSE 8000

CMD ["uv", "run", "uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
